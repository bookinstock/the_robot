# rake -T -all

desc "test"
task :test do
    puts "simple test"
end

namespace :backup do
    task :create do
        puts "create"
    end
    task :list do
        puts "list"
    end
    task :restore do
        puts "restore"
    end
end